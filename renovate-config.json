{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":dependencyDashboard"],
  "description": "Shared org baseline: raise PRs for GitHub and OSV vulnerability advisories, plus weekly PEP 621 lockfile maintenance for transitive Python dependencies. Consume via \"extends\": [\"github>ethereum-optimism/factory:renovate-config\"].",
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 4am on monday"]
  },
  "packageRules": [
    {
      "description": "Turn OFF all generic version updates.",
      "matchPackageNames": ["*"],
      "enabled": false
    },
    {
      "description": "Keep lockfile maintenance disabled outside PEP 621 projects.",
      "matchUpdateTypes": ["lockFileMaintenance"],
      "enabled": false
    },
    {
      "description": "Refresh PEP 621 lockfiles weekly to update transitive Python dependencies.",
      "matchManagers": ["pep621"],
      "matchUpdateTypes": ["lockFileMaintenance"],
      "enabled": true
    }
  ],
  "vulnerabilityAlerts": {
    "description": "Overrides the disable-all rule above so ONLY security fixes get PRs.",
    "enabled": true,
    "labels": ["security", "renovate-cve"]
  },
  "osvVulnerabilityAlerts": true,
  "dependencyDashboard": true,
  "dependencyDashboardTitle": "Renovate CVE Dashboard",
  "postUpdateOptions": ["gomodTidy"],
  "prConcurrentLimit": 0,
  "prHourlyLimit": 0,
  "rebaseWhen": "conflicted",
  "semanticCommits": "disabled",
  "commitMessagePrefix": "security:"
}

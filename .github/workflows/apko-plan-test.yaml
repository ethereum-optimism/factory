name: apko plan tests

on:
  pull_request:
    paths:
      - actions/apko-plan/**
      - .github/workflows/apko-plan-test.yaml
      - .github/workflows/melange-build.yaml
  push:
    branches:
      - main
    paths:
      - actions/apko-plan/**
      - .github/workflows/apko-plan-test.yaml
      - .github/workflows/melange-build.yaml

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@71cf2267d89c5cb81562390fa70a37fa40b1305e # v6
        with:
          persist-credentials: false
      - name: Test apko planner
        run: actions/apko-plan/apko-plan.test.sh
      - name: Test Melange runner selection
        run: python3 -m unittest discover -s actions/apko-plan -p 'test_*.py' -v
